# =============================================================================
# Cloudflare Pages Configuration for BeatSight Frontend
# =============================================================================
# This configures the deployment to Cloudflare Pages
# Docs: https://developers.cloudflare.com/pages/
# =============================================================================

name = "beatsight"
compatibility_date = "2024-01-01"

# Pages-specific settings
pages_build_output_dir = "dist"

# Environment variables are set in Cloudflare Dashboard or via wrangler
# Don't put secrets in this file!
